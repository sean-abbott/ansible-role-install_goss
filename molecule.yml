---
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: ansible-role-install_goss
      image: ubuntu
      image_version: latest
      ansible_groups:
        - group1
verifier:
  name: goss
