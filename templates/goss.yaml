---
file:
  /usr/local/bin/goss:
    exists: true
    owner: root
    group: root
    mode: "0755"
    sha256: "sha256:{{ goss[goss_version].sha256sum }}"

file:
  /usr/local/bin/dgoss:
    exists: true
    owner: root
    group: root
    mode: "0755"
