---
file:
  {{ goss_path }}/goss:
    exists: true
    owner: root
    group: root
    mode: "0755"
    sha256: "sha256:{{ goss[goss_version].sha256sum }}"
  {{ goss_path} }/goss:
    exists: true
    owner: root
    group: root
    mode: "0755"
